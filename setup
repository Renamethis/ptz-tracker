#!/bin/bash
os=($cat /etc/*-release)
if ! grep "Fedora" $os 
then
	str="apt-get"
else
	str="dnf"
fi
sudo $str upgrade --refresh
sudo $str install dnf-plugin-system-upgrade
sudo $str install python2 
sudo pip get-pip.py
sudo $str install -U virtualenv
sudo python2 setup.py $str
