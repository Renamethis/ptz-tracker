#!/bin/bash
os=($cat /etc/*-release)
echo os
if ! grep "Ubuntu" $os 
then
	str="dnf"
else
	str="apt-get"
fi
$str upgrade --refresh
$str install dnf-plugin-system-upgrade
$str install python2 
python2 get-pip.py
$str install -U virtualenv
python2 setup.py $str
